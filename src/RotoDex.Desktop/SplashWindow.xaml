<Window x:Class="RotoDex.Desktop.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Starting RotoDex..." Height="320" Width="480"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Icon="RotoDex.png">
    
    <Border CornerRadius="16" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="2">
        <!-- Glow effect behind the logo -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0">
                <!-- Logo with smooth breathing animation -->
                <Image Source="RotoDex.png" Width="110" Height="110" Margin="0,0,0,15" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <ScaleTransform x:Name="LogoScale" ScaleX="1" ScaleY="1"/>
                    </Image.RenderTransform>
                    <Image.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                    <DoubleAnimation Storyboard.TargetName="LogoScale" 
                                                     Storyboard.TargetProperty="ScaleX" 
                                                     From="0.96" To="1.04" Duration="0:0:1.2"/>
                                    <DoubleAnimation Storyboard.TargetName="LogoScale" 
                                                     Storyboard.TargetProperty="ScaleY" 
                                                     From="0.96" To="1.04" Duration="0:0:1.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Image.Triggers>
                </Image>

                <!-- Premium Title Branding -->
                <TextBlock Text="RotoDex" FontSize="36" FontWeight="ExtraBold" HorizontalAlignment="Center" Foreground="#3B82F6"/>
                <TextBlock Text="THE NEXT-GEN SAVE EDITOR" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#64748B" Margin="0,3,0,0"/>
            </StackPanel>

            <!-- Status and Progress Bar -->
            <StackPanel Grid.Row="1" Margin="30,0,30,30">
                <TextBlock x:Name="StatusText" Text="Initializing system cores..." FontSize="12" Foreground="#94A3B8" Margin="0,0,0,8"/>
                
                <ProgressBar x:Name="ProgressBar" Height="4" Minimum="0" Maximum="100" Value="0" 
                             Background="#1E293B" Foreground="#3B82F6" BorderThickness="0">
                    <ProgressBar.Template>
                        <ControlTemplate TargetType="ProgressBar">
                            <Grid>
                                <Border Background="{TemplateBinding Background}" CornerRadius="2"/>
                                <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" HorizontalAlignment="Left" CornerRadius="2"/>
                            </Grid>
                        </ControlTemplate>
                    </ProgressBar.Template>
                </ProgressBar>
            </StackPanel>
        </Grid>
    </Border>
</Window>
